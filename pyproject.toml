[project]
name = "archiver"
version = "0.1.0"
description = "automatic backup bot"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "aiogram>=3.17.0",
    "pydantic-settings>=2.7.1",
]
